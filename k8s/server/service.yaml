apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: fitnessapp
spec:
  selector:
    app: server
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
  type: NodePort
